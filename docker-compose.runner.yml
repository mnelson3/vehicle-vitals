services:
  github-runner-vehicle-vitals:
    build:
      context: .
      dockerfile: Dockerfile.runner
    container_name: github-runner-vehicle-vitals
    env_file:
      - .env.runner
    environment:
      REPO_URL: https://github.com/mnelson3/vehicle-vitals
      RUNNER_NAME: vehicle-vitals-docker-runner
      RUNNER_WORKDIR: /tmp/runner/work
      LABELS: self-hosted,ARM64,linux,ubuntu-latest,docker,vehicle-vitals
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - runner-work:/tmp/runner/work
      - /tmp:/tmp
    networks:
      - runner-network
    restart: unless-stopped

  # Optional: Runner with GPU support (uncomment if needed)
  # github-runner-gpu:
  #   image: myoung34/github-runner:latest
  #   container_name: vehicle-vitals-runner-gpu
  #   environment:
  #     REPO_URL: https://github.com/mnelson3/vehicle-vitals
  #     RUNNER_NAME: docker-runner-gpu-${HOSTNAME:-local}
  #     RUNNER_TOKEN: ${RUNNER_TOKEN}
  #     RUNNER_WORKDIR: /tmp/runner/work
  #     LABELS: self-hosted,ubuntu-latest,docker,vehicle-vitals,gpu
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock
  #     - runner-work:/tmp/runner/work
  #     - /tmp:/tmp
  #   networks:
  #     - runner-network
  #   restart: unless-stopped
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]

  # Monitoring container (optional)
  runner-monitor:
    build:
      context: .
      dockerfile: Dockerfile.monitor
    container_name: vehicle-vitals-runner-monitor
    ports:
      - "8082:8082"
    volumes:
      - ./monitoring/status.html:/usr/share/nginx/html/index.html:ro
    networks:
      - runner-network
    restart: unless-stopped
    depends_on:
      - github-runner-vehicle-vitals

volumes:
  runner-work:
    driver: local

networks:
  runner-network:
    driver: bridge