# Speed up npm by reducing network chatter and UI noise
progress=false
fund=false
audit=false

# Network optimization to prevent hanging
fetch-retries=2
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=20000
maxsockets=5

# Workspace optimization
prefer-dedupe=true
legacy-peer-deps=false
