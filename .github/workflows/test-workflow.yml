name: Test Workflow

on:
  workflow_dispatch:
    inputs:
      test_input:
        description: "Test input"
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo input
        run: echo "Input: ${{ inputs.test_input }}"